#include "StructureObserver.h"

namespace Screeps {
	StructureObserver::StructureObserver() {
	}
	StructureObserver::StructureObserver(emscripten::val structureObserver) {
	}
	int StructureObserver::observeRoom(SCREEPS_STR roomName) {
		return 0;
	}
}